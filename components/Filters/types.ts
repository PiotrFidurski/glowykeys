export interface FilterTypes {
  variant?: Array<string>;
  connectivity?: Array<string>;
  brand?: Array<string>;
  color?: Array<string>;
}

export interface SortTypes {
  order: string;
  type: string;
}
